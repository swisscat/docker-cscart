{
  "name": "swisscat/docker-cscart",
  "description": "An infrastructure for cs-cart around docker",
  "scripts": {
    "post-install-cmd": "cp -r var/config"
  },
  "extra": {
    "map": [
      [
        "docker-compose.yml",
        "docker-compose.yml"
      ],
      [
        "config",
        "config"
      ],
      [
        "var/toolbox.sh",
        "var/toolbox"
      ]
    ]
  }
}